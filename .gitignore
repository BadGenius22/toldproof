# Node / pnpm
node_modules/
.pnpm-store/
.pnpm-debug.log

# Next.js
.next/
out/
next-env.d.ts
.turbo/

# TypeScript
*.tsbuildinfo

# Env (NEVER commit secrets — see CLAUDE.md non-negotiables)
.env
.env.local
.env.*.local
.env.production
.env.development

# Move
# Move.lock + Published.toml are auto-generated but should be committed per Sui best practices
# (reproducible builds + tracking which package ID is live on each network).
move/**/build/

# Vercel
.vercel/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Sui keys (extra safety — keystore lives in ~/.sui already, but in case of mishaps)
*.keystore
*.key

# Internal — kept on disk for our own use, not for the public repo
buildplan.md
docs/design/
scratchpad/
scratchpad-v3/
IDEA_VALIDATION.md

